- Lets make with chroma a Upload file to ingest in a local database
    -Check if need to install something local or docker
        Lets make local DB
    -Check if we can easily upload a file with some front. maybe a AI Vercel SDK or similar.
    -which type of search types exist in rag ? thats something that would affect the arquitecture. and the approach
    -lets get two books and have a metadata filter for get from the text info from the specific book.

    First approach is wine reviews. would be fun
    